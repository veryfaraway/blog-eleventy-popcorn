---
layout: base.njk
permalink: /404.html
eleventyExcludeFromCollections: true
title: "페이지를 찾을 수 없어요"
---

{% set currentLang = page.lang or "ko" %}

<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-20 text-center">
  

  <div class="mb-10">
    <!-- <p class="text-sm font-semibold text-accent-500 mb-2">404</p> -->
    <h1 class="text-4xl md:text-4xl font-extrabold text-dark-900 mb-4">
      아마, 여기엔 아무도 상륙하지 않았을 거예요.
    </h1>
    <div class="mb-12 flex justify-center">
      <div class="relative max-w-xl w-full rounded-2xl overflow-hidden shadow-lg border border-gray-200 bg-gray-50">
        <img
          src="https://res.cloudinary.com/doal3ofyr/image/upload/v1770125384/library/errors/404_popcorn_island_real_cejp5o.jpg"
          alt="길을 잃은 팝콘 섬"
          class="w-full h-full object-cover"
          loading="lazy"
        >
      </div>
    </div>
    <p class="text-lg text-dark-700 mb-6">
      주소를 잘못 입력하셨거나, 이미 철수한 페이지일 수 있어요.<br />
      대신 다른 전선(?)으로 이동해 볼까요?
    </p>
  </div>

  <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
    <a href="/" class="px-6 py-3 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-colors">
      홈으로 돌아가기
    </a>
    <a href="/blog/" class="px-6 py-3 bg-white border-2 border-primary-600 text-primary-600 font-semibold rounded-lg hover:bg-primary-50 transition-colors">
      최신 글 보러가기
    </a>
  </div>

  <div class="border-t border-gray-200 pt-8 mt-8">
    <h2 class="text-xl font-bold text-dark-900 mb-4">최근에 쓴 글</h2>
    {% set recentPosts = collections.blog | slice(0, 5) %}
    {% if recentPosts and recentPosts.length > 0 %}
    <ul class="space-y-3 text-left max-w-xl mx-auto">
      {% for post in recentPosts %}
      <li class="flex items-start gap-3">
        <span class="mt-1 inline-block w-1.5 h-1.5 rounded-full bg-primary-500"></span>
        <div>
          <a href="{{ post.url }}" class="font-medium text-primary-700 hover:text-primary-800">
            {{ post.data.title }}
          </a>
          <p class="text-sm text-dark-600">
            {{ (post.data.description or "") | truncate(80, true, "…") }}
          </p>
        </div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="text-dark-600">아직 불러올 글이 없습니다.</p>
    {% endif %}
  </div>
</div>

